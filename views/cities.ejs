<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>DOMISILI</title>
  </head>
  <body>
    <p><%=JSON.stringify(town[0].name) %></p>
    <p><%=JSON.stringify(town[0].province) %></p>
    <br>
    <p><%=JSON.stringify(town[1].name) %></p>
    <p><%=JSON.stringify(town[1].province) %></p>
  </body>
</html>
