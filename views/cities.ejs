<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Kota</title>
  </head>
  <body>
    <table border="1">
      <td>Nama Kota</td>
      <td>Province</td>
    <% for( let i = 0; i < data_ambil.users.length; i++) { %>
      <tr>
<td><%= data_ambil.cities[i].name %></td>
<td><%= data_ambil.cities[i].province %></td>
</tr>
<%}%>
</table>

  </body>
</html>
